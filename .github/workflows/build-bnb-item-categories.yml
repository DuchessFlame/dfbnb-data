name: Build BnB Item Categories

on:
  workflow_dispatch:

  push:
    paths:
      - 'tsv/ALCH_Export_*.tsv'
      - 'tsv/KYWD_Export_*_Refs.tsv'
      - 'src/build_bnb_item_categories_json.py'

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v5

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: '3.12'

      - name: Run build script
        run: |
          mkdir -p dist
          python src/build_bnb_item_categories_json.py

      - name: Commit and push
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions@users.noreply.github.com"
          git add dist/
          if git diff --cached --quiet; then
            echo "No changes to commit."
          else
            git commit -m "Build BnB item categories data"
            git push
          fi
