# scratch file created during CI diagnosis on 2026-08-24; safe to delete.
# (Not tracked by git; the build workflows only `git add dist/`, so it is
# never committed by CI. Delete manually before running `git add .`.)
